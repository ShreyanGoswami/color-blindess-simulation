import React from "react";

import { Wrapper } from "./Header.styles";

const Header = () => (
    <Wrapper>
        <header className="d-flex flex-wrap pt-3 mb-4 border-bottom">
            <h2>Tutorial: Simulating color blindness</h2>
        </header>
    </Wrapper>
)

export default Header;